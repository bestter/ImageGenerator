🎯 **What:** The testing gap addressed is the lack of unit tests for the `HistoryItemCopiedEventArgs` class in `ImageGeneratorApp`. Tests have been added to verify that the constructor correctly assigns properties.

📊 **Coverage:** The new tests cover:
- Happy path: verifying that `Prompt` and `ModelName` properties correctly reflect the constructor arguments.
- Edge cases: handling of null strings for both `Prompt` and `ModelName`.

✨ **Result:** Test coverage is improved, providing a safety net to ensure `HistoryItemCopiedEventArgs` properties are reliably populated upon instantiation.
